# Levr

Capture-and-delegate tool: talk or type a thought, it gets classified into **Your 20%** (do it yourself) or **Delegated** (hand it off), with delegation history per person.

Status: real build in progress — see `PROJECT_STATE.md` for the current build state, env/DB notes, and next actions.

| What | Where |
|---|---|
| Product requirements (structure/data — wins on conflicts) | `docs/levr-requirements.md` |
| Design spec (visuals — tokens, type, components, motion) | `docs/DESIGN.md` |
| Approved visual prototype (open in a browser, phone-width) | `reference/levr-app-v2.html` |
| Original prototype (superseded) | `reference/levr-app-v1.html` |

Both prototypes are throwaway references — do not extend them. The real build's folder tree is at the bottom of `docs/DESIGN.md` §10.
